import { createTheme } from "@mui/material/styles"

export const strawTheme = createTheme({
    palette: {
        type:'light',
        primary: {
            main:'#C6A940'
        },
        secondary: {
            main: '#00ff00'
        }
    },
})